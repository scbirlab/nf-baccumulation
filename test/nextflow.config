params {
    
    sample_sheet = "sample-sheet.csv"
    inputs = "inputs"
    outputs = "outputs"

    positive = 'Doxycycline hyclate'
    negative = 'Rifampicin'
    grouping = 'sample_id plate_id'

    hit_grouping = 'compound_name'

}
