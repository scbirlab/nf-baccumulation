params {
    
    sample_sheet = "sample-sheet.csv"
    inputs = "inputs"
    outputs = "outputs"

    positive = 'Rifampicin' 
    negative = 'Doxycycline'
    grouping = 'sample_id plate_id'

    hit_grouping = 'experiment_id compound_id'

    export_layout = 'plate'

}
